syntax on  "Enables syntax highlighting for programming languages
set mouse=a  "Can use the mouse for all modes now
set ignorecase "use case insensitive search except when searching with capital letters
filetype indent plugin on "Tries to figure out the type of the file and its contents based on the name, and allows intelligent auto-indenting for each filetype
set showmatch "Shows matching brackets when you are over them
set autoindent  "If you're indented, new lines will also be indented
set smartindent  "Automatically indents lines after opening a bracket in programming languages
set tabstop=4  "How much space Vim gives to a tab
set number  "Allows line numbering
set smarttab  "Improves tabbing
set shiftwidth=4  "makes 1 tab equal to 4 spaces
set background=dark "use a dark background
set history=1000 "1000 commands saved in history
set virtualedit=onemore "Allow cursor to go one more after the last character
